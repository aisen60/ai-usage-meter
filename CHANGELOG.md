# Changelog

> 注：本项目已更名为 AI Usage Meter，工程 / 模块为 `AIUsageMeter`，Bundle ID 改为 `com.aisen.aiusagemeter`，仓库为 `aisen60/ai-usage-meter`。更名不改变版本号与既有功能语义。

## 0.3.3 - 2026-09-07

### 行为修正

- 修复首次未注册开机启动时被错误显示为构建不可用的问题。
- 将 macOS 登录项的 `.notFound` 状态视为普通未开启状态，允许用户正常启用。

### 工程与发布

- 移除 Debug 专用预览参数，改用 Testing 与 Release 构建配置。
- Release 构建统一支持 `arm64 + x86_64`。
- DMG 名称统一为 `AI Usage Meter.dmg`，并同步 GitHub Actions 发布流程。
- 将 App 版本更新为 `0.3.3`。
- 增加登录项状态和发布产物校验测试。

## 0.3.2 - 2026-09-05

### 新增

- 新增简体中文与英文界面切换，并允许用户在设置页控制登录后自动启动。
- 新增 GitHub Releases 更新检查：应用每五小时检查一次稳定版，下载 ZIP 与 SHA-256 校验文件后验证安装包，再替换当前应用。
- 新增 GitHub Actions 自动发布流程：从 `main` 手动触发后校验版本和更新日志、运行测试、构建 Universal ZIP、创建 tag 与公开 Release。

### 界面调整

- 设置页增加语言、开机启动和版本更新区域；更新可用时支持直接下载并安装。
- 重构中英文 README，并补充中英文隐私说明。

### 工程与发布

- 更新版本为 `0.3.2`，构建号为 `20260905`。
- 扩展语言、登录项、更新检查、Release 资产匹配和校验的确定性单元测试。
- 删除不再引用的历史设计资源。

## 0.3.1 - 2026-09-04

### 新增

- 根据 Cursor 与 ChatGPT 应用是否安装，分别展示对应的用量卡片；两者均未安装时提供空状态与支持应用说明。
- 新增应用图标资源，并补充 v0.3.1 空状态设计稿。

### 界面调整

- 应用名称更新为 AI Usage Meter，菜单栏前缀更新为 `AI`。
- 设置页允许关闭全部用量显示项目，菜单栏保留 `AI` 作为入口。
- Cursor On Demand 在状态栏与无障碍文案中改为显示已用美元金额。

### 工程与发布

- 更新 Cursor、ChatGPT 服务图标，以及应用产物名称相关的工程、测试宿主、README 和发布脚本路径。
- 扩展安装状态、空状态、菜单栏偏好和 On Demand 展示的确定性单元测试。

## 0.3.0 - 2026-08-29

### 新增

- Cursor 新增可选 On Demand 项目：读取 `spendLimitUsage.individualUsed` 与 `individualLimit`，以美元金额展示个人按量付费预算；仅在存在有效上限时显示，进度为已用金额除以上限。
- ChatGPT 同时展示 5 小时与 1 周两个剩余额度窗口，按窗口时长分类而非字段顺序；短周期显示重置时间，周周期显示重置日期。
- 状态栏与设置页扩展为五个独立显示项目：Cursor Models、Other Models、On Demand、ChatGPT 5 小时、ChatGPT 1 周，颜色依次为蓝、灰、紫、绿、绿。
- 显示设置从 v0.2.0 三字段自动迁移为五字段，旧开关保留，新增 On Demand 与 ChatGPT 5 小时默认开启。

### 界面调整

- 主弹层改为 Cursor 与 ChatGPT 两个展开卡片区，移除 Cursor「用量概览」折叠交互；On Demand 显示美元金额、紫色进度条与中文按量付费说明。
- 所有用户可见的 Codex 名称统一改为 ChatGPT，包括服务标题、设置页副标题、无障碍文案与未连接回退名称；内部类型与服务协议名保持不变。

### 行为修正

- Cursor 刷新失败但存在缓存时继续展示缓存，并明确标记「缓存数据」；On Demand 是否展示取决于该缓存中是否有有效上限。
- ChatGPT 刷新、协议或解析失败时两个窗口同时断开并显示中性 `0%`，不使用旧额度。
- 旧版缓存的 CursorUsage 不含 `individualUsed` 时，由上限与剩余反推，避免升级后短暂误报。

### 工程与发布

- 版本更新为 `0.3.0`，构建号 `20260829`。
- 扩展 Cursor On Demand、ChatGPT 双窗口解析、五项偏好迁移与状态降级的确定性单元测试。

## 0.2.0 - 2026-08-04

### 新增

- 新增设置页，可从菜单栏弹层底部的齿轮按钮进入，并通过左上角返回主页面。
- 新增状态栏实时预览，切换显示项目时可以立即确认菜单栏标签效果。
- 新增三个独立显示开关：Cursor Models、Other Models 和 Codex 本周剩余。
- 状态栏至少保留一个数值；最后一个已开启项目无法关闭，「CC」前缀始终显示。
- 显示设置通过 UserDefaults 保存在本机。首次升级到 v0.2.0 时默认显示全部项目。

### 界面调整

- 状态栏标签统一为「CC」前缀加彩色百分比胶囊：蓝色表示 Cursor Models 已用百分比，灰色表示 Other Models 已用百分比，绿色表示 Codex 本周剩余百分比。
- 弹层底部工具栏改为「设置｜刷新状态｜退出」三段式布局。
- 设置页、状态栏标签和图标按钮补充 VoiceOver 描述；无障碍朗读只包含当前启用的显示项目，并明确区分「已用」与「剩余」。

### 行为修正

- Cursor 刷新失败或凭据不可用但存在缓存时，继续展示缓存用量，并将连接状态明确标记为「缓存数据」。
- Codex 读取失败时仍立即断开并显示灰色 `0%`，不会把旧额度作为当前额度展示。
- 修复合法但三项全部关闭的持久化数据可绕过「至少保留一项」约束的问题；此类数据现在会回退到默认全开。
- 状态栏图片渲染失败时的文本降级内容会遵循显示设置，不再重新显示已关闭项目。

### 工程与发布

- 发行脚本版本更新为 `0.2.0`，并校验项目及最终安装包的构建号 `20260801`。
- 单元测试宿主禁用应用启动副作用，不再读取本机缓存、凭据或调用真实 Cursor/Codex 服务。
- 新增显示设置持久化、非法全关配置和 Cursor 缓存状态测试；当前测试共 17 项。
- 更新 README 与隐私说明，记录显示设置的本地存储位置和数据删除方式。
- 项目采用 MIT License 开源，项目自有开发技能与发布规范同步改为 MIT。

## 0.1.0 - 2026-07-19

- 首个私有发行版。
- 菜单栏同时展示 Cursor Composer 已用百分比和 Codex 周剩余百分比。
- 动态识别 Cursor/Codex 套餐名称和真实额度。
- 固定每 15 分钟自动刷新，支持手动刷新与退出。
- Release 首次运行自动注册 macOS 登录项。
- 提供 macOS 13+、`arm64 + x86_64` Universal ZIP。
- 增加无敏感信息的统一日志、隐私说明和解析单元测试。
